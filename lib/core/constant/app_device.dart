
/// Device Type enum
/// [mobile] for mobile device Limit 500px
/// [tablet] for tablet device Limit 800px
/// [desktop] for desktop device Limit 1200px
enum AppDevice {
  mobile,
  tablet,
  desktop,
}
